"""
URL parsing example.

Parses a URL string with `URL.parse()` and prints the decomposed components:
scheme, host, port, path, query, `request_path()`, and `is_ssl()`. Demonstrates
URL decomposition and error handling via the `URLParseError` union.
"""
