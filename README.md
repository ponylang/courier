# courier

courier is now part of ponyc's standard library as the `http_client` package.

courier was an HTTP/1.1 client for Pony.

For the current code, see the [`http_client` package in ponyc](https://github.com/ponylang/ponyc/tree/main/packages/http_client).
